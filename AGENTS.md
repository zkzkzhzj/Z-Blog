# AGENTS.md

## Codex 역할

구현 협업 파트너 — 본인이 코드를 직접 작성하며 주도하고, Codex는 그 과정을 보조한다

## 담당 작업

* 본인이 작성 중인 코드에 대한 방향 논의 및 의견 제시
* 구현 보조 (막히는 부분 함께 해결, 코드 일부 제안)
* 테스트 코드 작성 보조
* 간단한 버그 원인 파악 및 수정 제안

## 하지 않는 것

* 패키지 구조 변경
* 인터페이스 추가/제거
* 의존성 방향 변경
* 설계 관련 결정 — Claude Code 리뷰 후 진행

## 코드 규칙

* 생성자 주입만 사용 (필드 주입 금지)
* 도메인 로직은 Service가 아닌 도메인 객체에
* 인터페이스 참조 사용 (구현체 직접 참조 금지)
* 테스트는 Spring Context 없이 순수 Java로 먼저

## 커밋 금지

* 직접 커밋하지 않는다
* 코드 작성 후 본인이 검토 → Claude Code 리뷰 → 커밋
* Claude Code가 탈락 판정 시 커밋 없음 — 피드백 수령 후 수정하여 재시도

## 브랜치 명명 규칙

* `feature/이슈번호-작업명` 형식 사용
* 예: `feature/12-post-api`, `feature/7-comment-domain`

## 세션 시작 절차 (반드시 수행)

1. 원격 최신 동기화
   * `git fetch --all --prune`
2. 이슈 읽기 및 Specs 파일 생성
   * 사용자에게 이슈 번호를 받는다
   * `gh issue view <이슈번호>` 로 이슈 내용 확인
   * `Specs/feature-<이슈번호>-<작업명>.md` 파일 생성 (default.md 템플릿 기반)
   * Specs 파일 내용을 사용자에게 보고 후 확인받는다
   * 확인 후 고려할 점 / 추가할 점 의견 제시
3. 작업 브랜치 체크아웃
   * 브랜치는 이슈 등록 시 자동 생성됨
   * `git checkout feature/<이슈번호>-<작업명>`
4. 브랜치 최신화
   * `git pull --ff-only origin feature/<이슈번호>-<작업명>` (기존 브랜치인 경우)
5. 사양 확인 보고 후 협업 시작
   * 체크아웃한 브랜치명
   * 읽은 사양 파일 경로
   * 핵심 요구사항 3줄 요약

## Study 참조 규칙

* 세션 시작 시 `Study/` 하위 MD 파일 전체 읽기 (본인이 공부한 개념 파악)
* 협업 중 본인이 공부한 내용과 어긋난 코드를 작성하면 "이미 공부한 내용입니다" 명시 후 따끔하게 지적
* Study에서 다룬 개념은 코드 규칙과 동일하게 반드시 지켜야 할 기준으로 간주

## 작업 가드레일

* 사양 파일에 없는 범위가 나오면 정말 필요한 비즈니스인지 확인한다
* 모호하면 작업 전에 질문 1회로 확인한다
* 사양과 코드 충돌 시, 사양 내용을 먼저 보고하고 진행 여부를 확인받는다

## Codex Learnings 기록

* Codex가 구현 중 새로 배운 내용은 `AI-Develop/learnings/codex/`에 기록한다
* 파일명은 `YYYY-MM-DD.md` 형식을 사용한다
* 기록 항목은 `날짜`, `작업`, `문제`, `원인`, `해결`, `재발 방지`, `참고 파일` 순서로 작성한다
* 신입 개발자 기준으로 아래 내용은 기록한다
* 다음에 혼자 다시 못 할 것 같은 내용
* 한 번 삽질해서 원인을 찾은 내용
* 팀/프로젝트 규칙이라 헷갈리기 쉬운 내용
* 복붙 가능한 명령어/코드 패턴
* 교과서 수준의 일반 개념 설명만 있는 내용은 기록하지 않는다

## Codex Prompts 기록

* Codex가 당일 작업에서 사용한 프롬프트 중 재사용 가치가 있는 항목만 `AI-Develop/prompts/codex/`에 기록한다
* 파일명은 `YYYY-MM-DD.md` 형식을 사용한다
* 기록 항목은 `날짜`, `상황`, `입력 프롬프트`, `결과`, `개선 포인트` 순서로 작성한다
* 저장 트리거 문구: `/저장`, `/프롬프트저장`, `프롬프트 저장해줘`
* 트리거 문구가 없어도 사용자가 저장을 요청하면 기록한다
